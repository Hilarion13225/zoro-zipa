# API Configuration
# Development: le proxy Vite redirige /api vers le backend local
# Production: utilisez /api si l'application est servie via nginx ou via le reverse proxy local
VITE_API_URL=http://localhost:8080/api
